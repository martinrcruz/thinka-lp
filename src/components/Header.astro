---
import Icon from "./Icon.astro";
const { logoSrc = "/assets/images/logo.svg", showSettings = true } = Astro.props;
---
<!-- Header -->
<header class="header" role="banner">
  <nav class="nav" role="navigation" aria-label="Navegación principal">
    <div class="nav-container">
      <div class="nav-logo">
        <a href="/#" aria-label="Thinka - Inicio">
          <img src={logoSrc} alt="Thinka Logo" width="120" height="40">
        </a>
      </div>

      <div class="nav-menu" id="nav-menu">
        <slot />
      </div>

      <div class="nav-controls">
        {showSettings && (
          <>
            <!-- Language toggle - Currently disabled
            <button class="language-toggle" id="language-toggle" aria-label="Cambiar idioma">
              <span class="language-text">EN</span>
            </button>
            -->
            <button class="theme-toggle" id="theme-toggle" aria-label="Cambiar tema">
              <Icon name="sun" class="sun-icon" size={24} />
              <Icon name="moon" class="moon-icon" size={24} />
            </button>
          </>
        )}
        <button class="mobile-menu-toggle" id="mobile-menu-toggle" aria-label="Abrir menú móvil"
          aria-expanded="false">
          <span></span>
          <span></span>
          <span></span>
        </button>
      </div>
    </div>
  </nav>
</header>
