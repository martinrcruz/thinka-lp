---
import { SEO } from 'astro-seo';

interface Props {
	title: string;
	description?: string;
    image?: string;
    favicon?: string;
}

const { 
    title, 
    description = "Automatización de procesos con IA en Chile. Consultoría tecnológica especializada en reducción de costos, desarrollo de software y transformación digital.",
    image = "/assets/images/og-image.jpg",
    favicon = "/favicon.svg"
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const jsonLD = {
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "Thinka",
  "url": "https://thinka.cl",
  "logo": "https://thinka.cl/assets/logo/thinka-logo-cuadrado-fondo-uva.webp",
  "sameAs": [
    "https://www.linkedin.com/company/thinka",
    "https://twitter.com/thinka"
  ],
  "contactPoint": {
    "@type": "ContactPoint",
    "telephone": "+56912345678",
    "contactType": "customer service",
    "areaServed": "CL",
    "availableLanguage": "Spanish"
  },
  "description": description
};
---

<!doctype html>
<html lang="es" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href={favicon} />
		<meta name="generator" content={Astro.generator} />
        
        <SEO
            title={title}
            description={description}
            canonical={canonicalURL.toString()}
            openGraph={{
                basic: {
                    title: title,
                    type: "website",
                    image: image,
                    url: canonicalURL.toString(),
                },
                optional: {
                    description: description,
                    locale: "es_CL",
                    siteName: "Thinka - Consultoría de IA en Chile",
                }
            }}
            twitter={{
                card: "summary_large_image",
                site: "@thinka",
                creator: "@thinka",
                title: title,
                description: description,
                image: image,
            }}
            extend={{
                meta: [
                    { name: "keywords", content: "Automatización de procesos con IA, Consultoría IA Chile, Reducción de costos IA, Desarrollo de software con IA, IA en Chile, Optimización de modelos LLM" },
                ],
            }}
        />

        <!-- JSON-LD -->
        <script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLD)} />

        <!-- Google Fonts Inter -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

        <style is:global>
            /* Custom global styles if needed, mainly handled by Tailwind */
            body {
                font-family: 'Inter', sans-serif;
            }
        </style>
	</head>
	<body class="bg-white text-gray-900 antialiased dark:bg-gray-900 dark:text-gray-100">
		<slot />
	</body>
</html>
